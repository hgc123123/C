#include <iostream>
#include <string>
#include <vector>
using namespace std;

class Dynamic{
public:
    int maxChildNumOfArray(vector<int> vec);
    int maxContinueArraySum(vector<int> vec);
    int maxSumInRotateArray(int **n);
    int maxTwoArraySameOrderMethod(string str1, string str2);
    string str1,str2;
    vector<int> vec;
    int **n;
};
